# Punto de control de integridad de la Fase E.
#
# ALCANCE — leelo antes de interpretar un desajuste como corrupcion.
#
# Esto es un REGISTRO HISTORICO, no un sello vivo del arbol de trabajo. Congela el
# estado del corpus en el momento en que arranco la Fase E: 70 ficheros, partiendo
# del commit ef229b4. Algunos de esos 70 ya existian en ese commit y otros nacieron
# durante la propia Fase E.
#
# El corpus siguio evolucionando en las fases F y G. Por eso varios de estos hashes
# YA NO COINCIDEN con el contenido actual, y eso es lo correcto: si coincidieran,
# significaria que alguien reescribio el registro para que luciera limpio.
#
# 09_candidate_manifest.json y sha256-manifest.txt son igualmente historicos: sellan el
# candidato de Fase G, no el arbol de hoy. Tampoco coinciden ya, y por el mismo motivo.
#
# Para sellar el corpus ACTUAL hay una herramienta viva, que calcula sobre lo que hay en
# disco en este instante en vez de sobre una foto de hace meses:
#
#   node tools/evidence_hasher.js <fichero_1> [fichero_2 ...]
#
# La integridad de estos registros la aporta el propio historial de git: un manifiesto
# no puede contener su propio hash.

integrity_snapshot:
  kind: 'historical_checkpoint'
  phase: 'E'
  divergence_from_head: 'expected'
  source_commit: 'ef229b4a4914dad7596fabbce7375baaa56518c6'
  copied_files: 70
  sha256_manifest: 'f49300d5737752f2627dfba7abc2bbc65a70e21d1170dc35cb5b62b38510dbb4'
  manifest_file: 'sha256-manifest.txt'

live_corpus_seal:
  manifest_file: '09_candidate_manifest.json'
  entries: 116
  phase: 'G'
  outer_seal: 'git'
